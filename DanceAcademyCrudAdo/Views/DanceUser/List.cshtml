@model IEnumerable<DanceAcademyCrudAdo.Models.DanceUserModel>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

@Html.Raw(TempData["InsertMsg"])
@Html.Raw(TempData["UpdateMsg"])
@Html.Raw(TempData["DeleteMsg"])
@Html.Raw(TempData["DeleteErrorMsg"])

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StuName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StuAge)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StuParentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StuEmail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StuMobile)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StuName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StuAge)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StuParentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StuEmail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StuMobile)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id }, new {onclick = "return confirm('Do you really want to delete this record?')"})
        </td>
    </tr>
}

</table>
